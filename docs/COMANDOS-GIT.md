# COMANDOS GIT
...
git init
git add.
git commit -m "Proyecto Base"

git remote add origin <direccion_remota_github>
git remote add origin https://github.com/salgueironelson/frontend_inventario.git
git push origin master

git branch              permite ver las ramas que hay
...

## Creamos la rama develop 
...
git checkout -b develop

git push -u origin develop

...


## Git FLOW
...
git flow init           para iniciar git flow

...
